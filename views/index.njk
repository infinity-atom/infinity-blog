<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>infinity's blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <script src="/mode.js"></script>
</head>
<body data-bs-theme="dark">
    <nav class="navbar navbar-expand-sm border-bottom">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <img src="/InfinityAtom.png" style="width: 1.5em;">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-main">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbar-main">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="https://infatom.site">about</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/build-log">latest log</a>
                    </li>
                </ul>
                <button class="btn btn-default" id="btn-toggle-theme">
                    <i class="bi bi-circle-half"></i>
                </button>
            </div>
        </div>
    </nav>
    <div class="container-fluid p-2">
        <h1>New articles</h1>
        <ul class="list-unstyled" id="blog-articles">
            {%for article in articles%}
                <li class="mb-2">
                    <div class="card">
                        <div class="card-body">
                            <h4 class="lead"><b>{{ article.title }}</b> &bull; <span> {{ article.readableDate }} </span></h4>
                            {{ article.plaintext | truncate(200) }}
                        </div>
                        <div class="card-footer">
                            <a href="/article/{{ article.id }}">Read more</a>
                        </div>
                    </div>
                </li>
            {%endfor%}
        </ul>
    </div>
</body>
</html>